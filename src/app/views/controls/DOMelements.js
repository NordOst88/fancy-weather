const btn = document.getElementById('button_update');
const unitsButtons = document.querySelectorAll('.units__button');
const unitsBlock = document.getElementById('units');
const faceBtn = document.getElementById('drop-down-menu__face-button');
const arrowDown = document.getElementById('arrow-down');
const menu = document.getElementById('drop-down-menu');
const menuItems = document.querySelectorAll('.drop-down-menu__item');
const searchBtn = document.getElementById('search-input__button');
const searchInput = document.getElementById('search-input');
const inputBtn = document.getElementById('search-input__button');
const voiceBtn = document.getElementById('voice-button');

const DOM_ELEMENTS = {
  btn,
  unitsButtons,
  unitsBlock,
  faceBtn,
  arrowDown,
  menu,
  menuItems,
  searchBtn,
  searchInput,
  inputBtn,
  voiceBtn,
};

export default DOM_ELEMENTS;
